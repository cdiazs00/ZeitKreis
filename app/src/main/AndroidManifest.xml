<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZeitKreis"
        tools:targetApi="31">

        <meta-data
            android:name="DB_URL"
            android:value="${DB_URL}" />
        <meta-data
            android:name="DB_USER"
            android:value="${DB_USER}" />
        <meta-data
            android:name="DB_PASSWORD"
            android:value="${DB_PASSWORD}" />

        <activity
            android:name=".IniciarSesión"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuPrincipal" />
        <activity
            android:name=".Registro" />
        <activity
            android:name=".Agendas" />
        <activity
            android:name=".CalendarioGlobal" />
        <activity
            android:name=".Amigos" />
    </application>

</manifest>